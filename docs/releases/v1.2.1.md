# v1.2.1

## Fixed

- Release-note synchronization now waits for the Release workflow to complete successfully.
- `Check now` immediately shows its result instead of deferring it until recent user input is detected.
- Fixed user-activity detection so completed checks no longer fail silently.
