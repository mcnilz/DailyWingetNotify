# v1.2.0

## Added

- Automatic daily check notifications now wait until recent local user input is detected before showing, without rerunning the `winget` check or losing a deferred notification across app restarts.
